package com.slim.cft.resource;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCftResourceIT extends CftResourceTest {

    // Execute the same tests but in native mode.
}